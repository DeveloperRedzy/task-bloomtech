/**
 * Authentication Components Export Index
 *
 * Central export point for all authentication-related components.
 * Provides easy imports for pages and other components.
 */

export { LoginForm } from './LoginForm';
export { RegisterForm } from './RegisterForm';
export { UserProfile, CompactUserProfile } from './UserProfile';
export { default as AuthGuard } from './AuthGuard';
